{
  "name": "mds-testsuite",
  "version": "1.0.1",
  "description": "multiple data store test suite",
  "contributors": [
    "Andy Burke <andy.burke@mailbox.earth> (https://github.com/andyburke)"
  ],
  "main": "index.js",
  "scripts": {
    "docker_clean": "npm run docker_down && docker-compose rm -f",
    "docker_down": "docker-compose stop",
    "docker_up": "docker-compose up -d",
    "test": "npm run docker_up && tape test/**/*.js | tap-spec && npm run docker_clean"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^4.5.0",
    "multidatastore": "^2.2.0",
    "multidatastore-b2": "^1.0.2",
    "multidatastore-postgres": "^1.0.7",
    "multidatastore-rethinkdb": "^1.3.1",
    "multidatastore-s3": "^1.1.0",
    "tap-spec": "^4.1.1",
    "tape-async": "^2.3.0"
  }
}
